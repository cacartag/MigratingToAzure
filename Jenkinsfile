pipeline {
    agent "ubuntu_worker"
    stages {
        stage('build') {
            steps {
				sh 'echo "hello world from Jenkins"'
                sh 'python --version'
            }
        }
    }
} 


